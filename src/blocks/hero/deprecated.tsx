import { BlockDeprecation } from '@wordpress/blocks';
import { HeroAttributes } from 'src/blocks/hero';

const deprecated: ReadonlyArray<BlockDeprecation<HeroAttributes>> = [];

export default deprecated;
